name: 'safecd'
description: 'Reconcile git and safes'
inputs:
  rpc:
    description: 'Ethereum RPC url'
    required: true
  upload:
    description: 'Dry run'
    default: "false"
runs:
  using: 'docker'
  image: 'Dockerfile'
  env:
    RPC: ${{ inputs.rpc }}
    UPLOAD: ${{ inputs.upload }}